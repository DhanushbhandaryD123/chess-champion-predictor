# chess-champion-predictor
A real-time chess match winner prediction system using machine learning. It fetches live player data from Chess.com and predicts the likely winner based on player stats, rating history, and match performance. Built with React (frontend), Flask (backend), and Python ML models.
